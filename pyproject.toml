[project]
name = "pipeline"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "itk",
    "LongiSeg @ git+https://github.com/MIC-DKFZ/LongiSeg.git"
]

[project.scripts]
track = "src/track.py"
